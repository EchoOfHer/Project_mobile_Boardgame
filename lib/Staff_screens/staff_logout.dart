import 'package:flutter/material.dart';

class StaffLogout extends StatelessWidget {
  const StaffLogout({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Center(child: Text('Logout')));
  }
}
