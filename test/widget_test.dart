import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Empty test', (WidgetTester tester) async {
    // This test does nothing.
    // It's here to make sure the 'test' file doesn't cause errors.
    expect(1, 1);
  });
}
